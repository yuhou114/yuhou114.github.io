---
title: "使数组平衡的最少移除数目"
description: "本题目为 3634. 使数组平衡的最少移除数目"
date: 2026-06-09T09:38:46+08:00
lastmod: 2026-06-09T09:38:46+08:00
categories:
    - 算法-滑动窗口
tags:
    - 算法
draft: false
---

本题目为[3634. 使数组平衡的最少移除数目](https://leetcode.cn/problems/minimum-removals-to-balance-array/description/)

给你一个整数数组 nums 和一个整数 k。

如果一个数组的 最大 元素的值 至多 是其 最小 元素的 k 倍，则该数组被称为是 平衡 的。

你可以从 nums 中移除 任意 数量的元素，但不能使其变为 空 数组。

返回为了使剩余数组平衡，需要移除的元素的 最小 数量。

注意：大小为 1 的数组被认为是平衡的，因为其最大值和最小值相等，且条件总是成立。

***

- 示例 1:

> 输入：nums = [2,1,5], k = 2  
> 输出：1  
> 解释：  
> 移除 nums[2] = 5 得到 nums = [2, 1]。  
> 现在 max = 2, min = 1，且 max <= min * k，因为 2 <= 1 * 2。因此，答案是 1。  
 
- 示例 2:

> 输入：nums = [1,6,2,9], k = 3  
> 输出：2  
> 解释：  
> 移除 nums[0] = 1 和 nums[3] = 9 得到 nums = [6, 2]。  
> 现在 max = 6, min = 2，且 max <= min * k，因为 6 <= 2 * 3。因此，答案是 2。  
 
- 示例 3:

> 输入：nums = [4,6], k = 2  
> 输出：0  
> 解释：  
> 由于 nums 已经平衡，因为 6 <= 4 * 2，所以不需要移除任何元素。  
 


>[!tip]
> - 1 <= nums.length <= 10^5  
> - 1 <= nums[i] <= 10^9  
> - 1 <= k <= 10^5  

***

## 题解

### 核心思想

此类不定长滑动窗口（越短越合法）思路见[无重复字符的最长子串]({{< relref "post/算法-滑动窗口/无重复字符的最长子串" >}})

本题重点在于要找最大最小元素 =》排序，将头尾排除等效保留中间部分，因此为不定长滑窗问题。


```java
import java.util.Arrays;

class Solution {
    public int minRemoval(int[] nums, int k) {
        Arrays.sort(nums);
        int left = 0;
        int n = nums.length;
        int maxx = 0; //保留元素最大值

        for(int right = 0; right < n; right++){
            while((long) nums[left] * k < nums[right]){
                left++;
            }

            maxx = Math.max(maxx,right-left+1);
        }

        return n-maxx;
    }
}
```




















