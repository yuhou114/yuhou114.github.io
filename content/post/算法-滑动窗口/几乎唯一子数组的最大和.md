---
title: "几乎唯一子数组的最大和"
description: "本题目为 2841.几乎唯一子数组的最大和"
date: 2026-05-23T21:39:02+08:00
lastmod: 2026-05-23T21:39:02+08:00
categories:
    - 算法-滑动窗口
tags:
    - 算法
draft: false
---

本题目为[2841.几乎唯一子数组的最大和](https://leetcode.cn/problems/maximum-sum-of-almost-unique-subarray/description/)

给你一个整数数组 nums 和两个正整数 m 和 k 。

请你返回 nums 中长度为 k 的**几乎唯一子数组的最大和**，如果不存在几乎唯一子数组，请你返回 0 。

如果 nums 的一个子数组有至少 m 个互不相同的元素，我们称它是**几乎唯一**子数组。

子数组指的是一个数组中一段连续**非空**的元素序列。

***

- 示例 1：

> 输入：nums = [2,6,7,3,1,7], m = 3, k = 4  
> 输出：18  
> 解释：总共有 3 个长度为 k = 4 的几乎唯一子数组。分别为 [2, 6, 7, 3] ，[6, 7, 3, 1] 和 [7, 3, 1, 7] 。这些子数组中，和最大的是 [2, 6, 7, 3] ，和为 18 。  

- 示例 2：

> 输入：nums = [5,9,9,2,4,5,4], m = 1, k = 3  
> 输出：23  
> 解释：总共有 5 个长度为 k = 3 的几乎唯一子数组。分别为 [5, 9, 9] ，[9, 9, 2] ，[9, 2, 4] ，[2, 4, 5] 和 [4, 5, 4] 。这些子数组中，和最大的是 [5, 9, 9] ，和为 23 。  
 
- 示例 3：

> 输入：nums = [1,2,1,2,1,2,1], m = 3, k = 3  
> 输出：0  
> 解释：输入数组中不存在长度为 k = 3 的子数组含有至少  m = 3 个互不相同元素的子数组。所以不存在几乎唯一子数组，最大和为 0 。  
 

>[!tip]
> - 1 <= nums.length <= 2 * 10^4
> - 1 <= m <= k <= nums.length
> - 1 <= nums[i] <= 10^9

*** 

## 题解

### 核心思想

通用思路见[定长子串中元音的最大数目]({{< relref "post/算法-滑动窗口/定长子串中元音的最大数目" >}})

本题重点在于能想到使用HashMap去判断唯一子数组，其他问题不大。

```java
import java.util.HashMap;
import java.util.List;
import java.util.Map;

class Solution {
    public long maxSum(List<Integer> nums, int m, int k) {
        Integer[] numss = nums.toArray(Integer[]::new);
        long maxx = 0;
        long temp = 0;
        Map<Integer,Integer> count = new HashMap<>();

        for(int i = 0; i < numss.length; i++){
            // 1. 进入窗口
            temp += numss[i];
            count.merge(numss[i], 1, Integer::sum);

            int left = i - k + 1;
            if(left < 0){
                continue;
            }

            // 2. 更新答案
            if(count.size() >= m){
                maxx = Math.max(maxx, temp);
            }

            // 3. 离开窗口
            int out = numss[left];
            temp -= out;
            int outC = count.get(out);

            if(outC > 1){
                count.put(out, outC - 1);
            }else{
                count.remove(out);
            }
        }
        return maxx;
    }
}
```